#include "utility.hpp"

using namespace evaluator::util;

/*
constexpr double evaluator::util::ConvertDegToRad(Operand deg)
{
	return deg * (PI / 180);
}

constexpr double evaluator::util::ConvertRadToDeg(Operand rad)
{
	return rad * (180 / PI);
}
*/
